# Nao ha workspace de verdade aqui: este arquivo existe porque o pnpm 10 passou
# a ler onlyBuiltDependencies daqui, e nao mais so de package.json. A lista fica
# duplicada de proposito, para funcionar em qualquer versao do pnpm que a Vercel
# resolva usar.
onlyBuiltDependencies:
  - core-js
  - esbuild
  - protobufjs
  - sharp
  - unrs-resolver
